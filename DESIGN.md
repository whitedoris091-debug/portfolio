# DESIGN.md

## 气质与意象

清爽的白底如晨雾中的湖面，蓝色光晕若隐若现地弥散在背景中，毛玻璃卡片微微透出底下的光晕。整体明亮、克制、呼吸感强，参考 iOS/macOS 设计语言——白色底上模糊的彩色光斑，卡片悬浮其上。蓝色作为全站核心强调色贯穿，酒红极少量撞色点缀。

关键词：亮色白底、弥散蓝雾、毛玻璃悬浮、湖蓝强调、克制。

## 配色方案

| Token | 值 | 用途 |
|-------|------|------|
| 页面背景 | `#F8FAFD` | 极浅蓝灰白主背景 |
| 区块/卡片背景 | `rgba(255, 255, 255, 0.7)` + `backdrop-filter: blur(16px) saturate(1.2)` | 纯白毛玻璃 |
| 卡片hover底色 | `rgba(22, 104, 159, 0.04)` | 极淡蓝hover底色 |
| 卡片边框 | `rgba(22, 104, 159, 0.1)` | 淡蓝色边框 |
| 文字主色 | `#1A2332` | 深蓝黑标题/正文 |
| 文字辅色 | `#6B7A8D` | 蓝灰描述/副标题 |
| 主强调色 | `#16689F` | 湖蓝链接/按钮/标签底色（全站核心） |
| 强调色亮 | `#1E7AB5` | hover状态 |
| 标签文字 | `#16689F` | 技能标签 |
| 标签背景 | `rgba(22, 104, 159, 0.08)` + `border rgba(22, 104, 159, 0.12)` | 标签底色 |
| 撞色点缀 | `#bf0a2d` | 酒红仅"进行中"标签等极少量点缀 |
| 暖色点缀 | `#fbec8e` | 鹅黄仅装饰光晕 |
| 弥散光1 | `#728DC1` | 中蓝光晕，opacity 0.12 |
| 弥散光2 | `#A4C9ED` | 淡蓝光晕，opacity 0.12 |
| 弥散光3 | `#C8EDEC` | 青绿光晕，opacity 0.08（极少用） |
| 分割线 | `rgba(22, 104, 159, 0.08)` | 细线 |
| 导航栏背景 | `rgba(248, 250, 253, 0.8)` + `blur(20px) saturate(1.2)` | 顶栏 |
| 卡片阴影 | `0 1px 3px rgba(22,104,159,0.06), 0 4px 16px rgba(22,104,159,0.04)` | 默认 |
| 卡片hover阴影 | `0 8px 30px rgba(22,104,159,0.1)` | hover增强 |

## 字体排版

- **标题**：Space Grotesk 700/600
- **正文**：Inter 400/500/600 + Noto Sans SC
- **代码/标签**：JetBrains Mono 400/500
- 字号：Hero名 56px/700、区标题 32px/600、卡片名 20px/600、描述 14px/400、标签 12px/500、正文 16px/400
- 行高：英文 1.6，中文 1.8

## 动效与交互

- 页面切换：淡入淡出 300ms，opacity 0→1 + translateY 10px→0
- 卡片hover：边框变湖蓝 + translateY(-4px) + 阴影增强 + 封面截图淡出视频播放
- 滚动渐入：IntersectionObserver，卡片从下方淡入，每张延迟 100ms
- 搜索：展开 300ms 从右滑入，匹配卡片淡入，不匹配 opacity 0.2 + blur 2px
- 详情页：从右侧滑入 500ms ease-out，hash路由
- Lightbox：全屏亮色遮罩 + 居中放大图，背景 blur 4px，300ms ease-out

## 设计禁忌

- 禁止用暗色/深色背景，必须用 #F8FAFD 亮色底
- 禁止用纯黑 #000000 做页面背景
- 大面积颜色必须是浅色/白色，蓝色只做强调
- 禁止卡片放两个媒体区域，只能一个封面区叠放
- 禁止用展开式详情，必须用 hash 路由详情页
- 禁止用系统默认字体
- 禁止粒子动画或炫酷特效
- 禁止忘记弥散光晕和毛玻璃质感
